# <%- tag %>

These methods can be accessed from `<%- key %>` property of a `BungieApi` instance.

```javascript
import { BungieApi } from 'bungie.net'

// Create the root API instance
const api = new BungieApi(options)

// Access the method
await api.<%- key %>.<%- methods[0].identifier %>(<%- methods[0].arguments.map(arg => arg.identifier).join(', ') %>)
```
<%_ for (const method of methods) { _%>

## `<%- method.identifier %>(<%- method.arguments.map(compileJSDocArgument).join(', ') %>)`
  <%_ if (method.description) { _%>

<%- method.description %>
  <%_ } _%>

  <%_ for (const arg of method.arguments) { _%>
- `<%- compileJSDocArgument(arg) %>` <%- compileMarkdownArgumentType(arg) %><% if (arg.enum) { %> See [<%- arg.enum %>](./Enums.md#<%- arg.enum %>) enum.<% } %> <%- arg.description %>
  <%_ } _%>
- Returns: `<Promise>`

URL: `<%- method.request.method %> /Platform<%- method.request.path %>`

Source: [<%- method.swagger.operationId %>](https://bungie-net.github.io/#<%- method.swagger.operationId %>)
<% } %>
